name: Run tests on Windows
on:
  workflow_call:
  push:
  pull_request:
  merge_group:

jobs:
  test:
    strategy:
      fail-fast: false
    runs-on: ubuntu-22.04  # Use 22.04 with godot 4
    # runs-on: windows-latest
    steps:
      # Clone this repository.
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true
          # with:
          #   lfs: true
          token: ${{ secrets.PRIVATESURFACER2ACCESSTOKEN}}

      # Setup dependencies.
      - name: Setup godot-cpp
        uses: ./godot-cpp/.github/actions/setup-godot-cpp
        with:
          platform: linux
          # platform: windows
          em-version: 3.1.62

      # Build Godot Editor.
      - name: Build Godot Editor
        shell: sh
        run: |
          cd godot
          scons target=editor platform=linux

      # Build GDExtension.
      - name: Build GDExtension Debug Build
        shell: sh
        run: |
          scons optimize=debug target=template_debug platform=linux arch=x86_64 precision=single custom_api_file=./demo/extension_api.json
        #   scons optimize=debug target=template_debug platform=windows arch=x86_64 precision=single optimize=debug custom_api_file=./demo/extension_api.json

      - name: Run tests
        run: |
          ./godot/bin/godot.linuxbsd.editor.x86_64 --headless --quit --path ./demo
        # ./godot/bin/godot.windows.editor.x86_64 --headless --quit --path ./demo

      # TODO: Only needed for Windows?
      # # Clean up compilation files.
      # - name: Windows - Delete compilation files
      #   shell: pwsh
      #   run: |
      #     Remove-Item bin/* -Include *.exp,*.lib,*.pdb -Force

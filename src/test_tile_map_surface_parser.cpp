#ifdef DEBUG_ENABLED

#include "test_runner.h"
#include "tile_map_surface_parser.h"

test_space([]() {
	describe("TileMapSurfaceParser", []() {
		it("TODO", []() {
			// TODO
			// Expect(Surface::get_normal_from_side(Surface::Side::FLOOR),
			// 	   Vector2(0, -1));
		});
	});
});

#endif // DEBUG_ENABLED

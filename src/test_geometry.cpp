#ifdef DEBUG_ENABLED

#include "geometry.h"
#include "test_runner.h"

test_space([]() {
	describe("Geometry", []() {
		it("TODO", []() {
			// TODO
			// Expect(Surface::get_normal_from_side(Surface::Side::FLOOR),
			// 	   Vector2(0, -1));
		});
	});
});

#endif // DEBUG_ENABLED
